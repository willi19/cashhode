#include <iostream>
#include <vector>
using namespace std;
void greedy_select(int num_photos, int num_V, int num_H, vector<char> orient,  vector<vector<int>> V){
    
    for(int i=0 ; i<num_photos ; i++){
        if(orient[i] == 'V'){

        }
        else if(orient[i] == 'H'){

        }
        else{
            cout<<"orient[i] neither V, H\n";
            return;
        }
    }
}
int main(){
    int A;
    cin>>A;
}